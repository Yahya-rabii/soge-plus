version: '3'

services:
  minio:
    image: 'bitnami/minio:latest'
    ports:
      - '9000:9000'
      - '9001:9001'
    networks:
      - minionetwork
    volumes:
      - 'minio_data:/data'
    environment:
      - MINIO_ROOT_USER=noxideuxTheGoat
      - MINIO_ROOT_PASSWORD=noxideux11102001noxideux
      - MINIO_DEFAULT_BUCKETS=appccbucket

networks:
  minionetwork:
    driver: bridge

volumes:
  minio_data:
    driver: local