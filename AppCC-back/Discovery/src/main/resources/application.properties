spring.application.name=Discovery
server.port=8761

#so that it does not register itself as a client
eureka.client.fetch-registry=false

#so that it does not register itself in the service registry
eureka.client.register-with-eureka=false


