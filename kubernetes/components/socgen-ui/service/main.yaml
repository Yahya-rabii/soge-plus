apiVersion: v1
kind: Service
metadata:
  name: socgen-ui-service
  labels:
    app: socgen-ui
spec:
  selector:
    app: socgen-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  #type: NodePort