apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: sgma-system
  name: discovery-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: discovery-service
  template:
    metadata:
      labels:
        app: discovery-service
    spec:
      containers:
      - name: discovery-service
        image: salaheddine122/soge-system-discovery-service:DISCOVERY-SERVICE-v1.1.0
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: discovery-service-configmap
          - configMapRef:
              name: active-profile-configmap
        ports:
        - containerPort: 8761

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8761
          initialDelaySeconds: 10
          periodSeconds: 10

        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8761
          initialDelaySeconds: 10
          periodSeconds: 10
        #resources:
         # requests:
          #  memory: "64Mi"
           # cpu: "50m"
          #limits:
           # memory: "128Mi"
            #cpu: "100m"
      imagePullSecrets:
        - name: regcred